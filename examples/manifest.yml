# Valid manifest

steps:
  - copy:
    - src: foo
      dst: /foo
    link:
    - src: foo
      dst: ~/foo
    - src: bar
      dst: ~/test/bar
    tags: [ a, b ]

  - run:
    - src: baz
      postfix: arg1 arg2 arg3
    tags: [ c ]
